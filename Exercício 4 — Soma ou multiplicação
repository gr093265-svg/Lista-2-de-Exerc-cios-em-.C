#include <stdio.h>

int main() {
    int a, b, c;

    printf("Digite A e B: ");
    scanf("%d %d", &a, &b);

    if (a == b) {
        c = a + b;
    } else {
        c = a * b;
    }

    printf("Resultado (C): %d\n", c);

    return 0;
}
